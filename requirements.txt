# VideoAudio Batch Converter - Dependencies
# 
# Install command: pip install -r requirements.txt

# Progress bar library
tqdm>=4.64.0

# GUI dependencies (tkinter is built-in with Python)
# tkinter - GUI framework (built-in)

# FFmpeg Python binding (optional if ffmpeg is already installed on system)
# ffmpeg-python>=0.2.0

# The following are Python standard libraries, no additional installation needed
# pathlib      # Path handling (built-in Python 3.4+)
# configparser # Configuration file management (built-in)
# argparse     # Command line argument parsing (built-in)
# logging      # Logging system (built-in)
# subprocess   # Subprocess management (built-in)
# os           # Operating system interface (built-in)
# sys          # System-related parameters (built-in)
# time         # Time handling (built-in)
# typing       # Type hints (built-in Python 3.5+)